# STRUTTURA:
# Select
# From
# Where
# Group by => tutte le colonne del SELECT (che non sono aggregate, come SUM, AVG, COUNT, ecc.) devono comparire nel GROUP BY.
# Having => per filtrare SOLO DOPO group by, se non ho group by non lo uso
# Order by
# Limit
